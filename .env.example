# ============================================================================
# Configuration de l'extension Validation Documentaire BIM
# Copier ce fichier en .env et renseigner les valeurs
# ============================================================================

# ── Mode de fonctionnement ──────────────────────────────────────────────────
# 'production' : Connecté à Trimble Connect (dans l'iframe)
# 'demo'       : Mode démonstration avec données fictives
VITE_APP_MODE=demo

# ── Backend Proxy ──────────────────────────────────────────────────────────
# URL du backend proxy (Vercel Serverless, Express, etc.)
# En développement local : http://localhost:3000/api
# En production : https://votre-backend.vercel.app/api
VITE_BACKEND_URL=http://localhost:3000/api

# ── Trimble Identity (OAuth 2.0) ──────────────────────────────────────────
# Ces valeurs sont côté BACKEND uniquement (ne PAS exposer le client_secret)
TRIMBLE_CLIENT_ID=your_trimble_client_id
TRIMBLE_CLIENT_SECRET=your_trimble_client_secret
TRIMBLE_REDIRECT_URI=http://localhost:3000/auth/callback

# ── Environnement Trimble ──────────────────────────────────────────────────
# 'production' ou 'staging'
TRIMBLE_ENVIRONMENT=production

# ── Base de données (Turso / SQLite) ──────────────────────────────────────
TURSO_DATABASE_URL=libsql://your-db.turso.io
TURSO_AUTH_TOKEN=your_turso_auth_token

# ── Frontend URL (pour CORS) ─────────────────────────────────────────────
FRONTEND_URL=http://localhost:5173

# ── Port du backend ──────────────────────────────────────────────────────
PORT=3000
